export enum POSITION_STATUS {
  BLACK = 'BLACK',
  WHITE = 'WHITE',
  NONE = 'NONE',
}

export enum PLAYER {
  BLACK = 'BLACK',
  WHITE = 'WHITE',
}

export enum GAMESTATUS {
  ACTIVE = 'ACTIVE',
  WON = 'WON',
  DRAWN = 'DRAWN',
}

export enum ACTION {
  JOIN = 'JOIN',
  REENTER = 'REENTER',
  RETRIEVE = 'RETRIEVE',
  LEAVE = 'LEAVE',
  MOVE = 'MOVE',
  REST = 'REST',
  RESET = 'RESET',
}

export enum PLAYER_STATE {
  RESTING = 'RESTING',
  PLAYING = 'PLAYING',
}
